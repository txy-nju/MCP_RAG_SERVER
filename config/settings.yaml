llm:
  provider: azure
  model: gpt-4o-mini

embedding:
  provider: openai
  model: text-embedding-3-small

vector_store:
  provider: chroma
  collection: default

retrieval:
  top_k: 5

rerank:
  provider: none

evaluation:
  backend: custom

observability:
  log_level: INFO
  trace_file: logs/traces.jsonl
